#include<iostream>
using namespace std;
int main()
{
    int a,b;
    cout<<"enter a and b";
    cin>>a>>b;

    if (a>b)
    {
        cout<<"max is "<<a;

    }
    else
    {
        cout<<"sahi kiya yaar";
        }
    return 0;
}
// if there are 2 conditions in if or else then braces are required otherwise optional.

